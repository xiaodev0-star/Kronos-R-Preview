# Kronos-R-Preview 改进路线图

> 基于"LLM 范式 + 选择性预测"的系统性改进方案。
> **部分改进已实施**——参见 `TEMP/EXP_2026_06_17_BERT_CALIBRATION/`（BERT 校准 session）
> 本文档讨论的是未来改进方向。

---

## 0. 核心理念

从信息论角度看，NLP 式因果模型在股价预测上总体不可能优于随机游走。但总体准确率低于随机游走，不代表模型在所有时刻都没有信息量——它的预测分布中仍可能存在 **regime-specific** 的可利用信号。

核心策略：**不是要求模型在所有时刻都准确，而是让它学会识别"什么时候我比较有把握"，只在高置信时刻输出预测，其余时刻弃权。** 再将筛选后的预测与传统计量模型（ARIMA/GARCH/XGBoost）对比胜率。

实现路径：遵循 LLM 训练范式，将结构学习与决策策略分离。

```
┌──────────────────────────────────────────────────────────────┐
│  Stage 1: PreTrain（因果预训练）                                │
│  目标: 学会"读"市场的结构和模式                                  │
│  修改: heteroscedastic 预测头（均值+方差）                       │
│  不涉及: 弃权、置信度、RL                                       │
├──────────────────────────────────────────────────────────────┤
│  Stage 2: SFT（有监督微调）[可选]                               │
│  目标: 教模型初步区分"清晰信号"与"模糊信号"                      │
│  用 ensemble 正确答案构造三类标签: 涨/跌/不确定                   │
├──────────────────────────────────────────────────────────────┤
│  Stage 3: RL（强化学习）                                       │
│  目标: 学习最优交易策略——何时预测、何时弃权                        │
│  奖励: 风险调整收益 - 交易频率惩罚                               │
└──────────────────────────────────────────────────────────────┘
```

**为什么分离训练优于一体化训练：**

- PreTrain 阶段加入弃权机制 → 模型还没学会"理解"市场，就已经学会了"沉默"。一个还没学好语言的学生，被告知"不确定就说不知道"，他会发现"不知道"的期望风险最低，于是大量使用。
- 多目标 loss 在训练早期造成梯度竞争：`L_structure` 说"你要学会识别这个形态"，`L_abstain` 说"这个样本波动率高你应该弃权"——模型无法区分"我真的不确定"和"我还没学会"。
- PostTrain (RL) 是引入弃权的最佳时机：弃权本质上是序列决策问题（"这次出手对整体收益曲线有什么影响"），这正是 RL 擅长的。

---

## 1. PreTrain 阶段改进

### 1.1 Heteroscedastic 预测头（均值 + 方差）

**目标**：让幅度回归头同时输出预测值和不确定性估计，为后续 RL 阶段提供天然的置信度信号。

**现状**：幅度回归头输出单个值 `Δp`。

**改进**：输出两个值 `mean` 和 `log_variance`，用负对数似然（NLL）作为 loss。

```
现状:  Δp = 0.3%          →  只输出一个数字
改进:  Δp ~ N(0.3%, σ=1.2%)  →  同时输出不确定性估计 σ
```

**数学形式**：

```python
# 模型输出: predicted_mean, predicted_log_var
# 目标: target_price_change

def heteroscedastic_nll(predicted_mean, predicted_log_var, target):
    """负对数似然 — 最大似然估计天然支持双输出"""
    var = torch.exp(predicted_log_var)
    loss = 0.5 * torch.log(var) + 0.5 * (target - predicted_mean) ** 2 / var
    return loss.mean()
```

**为什么这不需要额外的 loss 设计**：

- 最大似然估计中，同时优化均值和方差是自然的——方差大时 loss 对均值误差的惩罚自动降低
- 训练动力学上**更稳定**：大误差样本的 σ 自动增大，降低其梯度贡献（天然的自适应样本加权）
- 不改变 PreTrain 的核心目标（仍然是最大似然），只是让模型同时学到"知道什么"和"有多确定"

**对模型架构的改动**：

```
现有: RMSNorm → Linear(256 → 1026) → logits [方向]
新增: RMSNorm → Linear(256 → 2) → [mean, log_var] [幅度]

方向预测头（分类）→ 保持不变，输出 next-token logits
幅度预测头（回归）→ 改为输出 mean + log_var
```

**PostTrain 可利用性**：训练完成后，`σ`（或 `exp(0.5 * log_var)`）直接作为 RL agent 的状态特征——σ 大说明模型对幅度不确定，σ 小说明模型有把握。

### 1.2 PreTrain 阶段不做（及原因）

| 不做的事 | 原因 |
|---------|------|
| 加入"弃权"类/标签 | 模型还没学会理解市场就学会了沉默，弃权率会虚高 |
| 熵正则化 | 多目标 loss 在训练早期造成梯度竞争 |
| 集成模型 | 训练成本 N 倍，且此时需要的是结构学习而非不确定性校准 |
| RL | 模型还没有足够的市场理解来做有意义的决策 |

---

## 2. SFT 阶段改进（可选桥接）

### 2.1 构造三类标签的有监督微调数据

**目标**：在 RL 之前，先用有标签数据教模型"清晰行情→自信预测，震荡行情→弃权"。

**标签构造方法**（在 held-out 训练数据上离线计算）：

```
方法 A — 多模型 ensemble 事后标记:
  1. 用不同种子训练 N 个模型（N=3~5）
  2. 对同一输入，收集所有模型的预测方向
  3. 若 N 个模型方向一致 → label = 多数方向（涨/跌）
  4. 若方向分裂 → label = 不确定

方法 B — 基于后续收益标记:
  1. 对每个时间步 t，计算后续 N 根 K 线的实际方向
  2. 若 |Δp| > 阈值且方向清晰 → label = 涨/跌
  3. 若 |Δp| < 噪声阈值 → label = 不确定

方法 C — 基于波动率 regime:
  1. 计算 rolling volatility
  2. 低波动 + 趋势明确 → label = 涨/跌
  3. 高波动 + 无方向 → label = 不确定
```

**推荐方法 A**，因为 ensemble 分歧是最直接的"模型群体认为信号是否清晰"的度量。

### 2.2 SFT 训练细节

```python
# 三类交叉熵: [涨, 跌, 不确定]
# 方向预测头输出维度从 1026 → 1027 (新增一个 abstain token ID)

# 或者更优雅地，在方向预测头之外加一个独立的弃权判断头:
class AbstainHead(nn.Module):
    """独立的弃权判断头，与方向预测头解耦"""
    def __init__(self, dim):
        self.proj = nn.Linear(dim, 3)  # [确定涨, 确定跌, 不确定]

    def forward(self, hidden_state):
        return self.proj(hidden_state)  # softmax over 3 classes
```

**训练策略**：冻结 Transformer 主干 + 幅度头，仅训练 AbstainHead。这样不会破坏 PreTrain 学到的市场理解。

---

## 3. PostTrain (RL) 阶段改进

### 3.1 RL 问题建模

```
状态空间 (State):
  - 当前 K 线序列的 Transformer hidden state
  - heteroscedastic σ（不确定性估计）
  - 已持仓状态 / 持仓收益
  - 近期弃权率（滑动窗口）
  - 当前波动率 regime

动作空间 (Action):
  - 做多 (Long)
  - 做空 (Short)
  - 持仓不变 (Hold)
  - 弃权 / 不交易 (Abstain)

奖励函数 (Reward):
  R = risk_adjusted_return - λ · trade_frequency

  其中:
  - risk_adjusted_return: 本次交易的实际收益（或夏普比率的增量形式）
  - trade_frequency: 惩罚过于频繁的交易（降低交易成本侵蚀）
  - λ: 超参数，控制"积极交易"与"谨慎弃权"的平衡

  关键设计: 弃权的 R = 0（不亏不赚）
  → 模型通过训练发现: 低置信时弃权的累积收益 > 乱猜的累积收益
  → 自发学会弃权策略
```

### 3.2 RL 算法选择建议

| 算法 | 适用性 | 理由 |
|------|--------|------|
| PPO | ★★★ 推荐 | 稳定，适合离散动作空间，LLM alignment 的标准选择 |
| DPO (Direct Preference Optimization) | ★★★ 推荐 | 不需要训练 reward model，直接从偏好对学习 |
| SAC | ★★ | 适合连续动作，但你的动作空间是离散的 |
| REINFORCE | ★ | 最简单但方差大，不推荐 |

**DPO 特别适合的场景**：可以离线构造"好决策 vs 差决策"的偏好对——

```
偏好对构造:
  "在 2023-06-15 高波动环境下弃权" > "在 2023-06-15 高波动环境下做多"（因为做多亏了）
  "在 2023-07-20 趋势行情下做多" > "在 2023-07-20 趋势行情下弃权"（因为做多赚了）
```

### 3.3 RL 训练的策略细节

```
与标准 LLM RLHF 的对应关系:

  LLM:  PreTrain(语言模型) → SFT(指令跟随) → RLHF(人类偏好)
  Kronos: PreTrain(市场理解) → SFT(弃权初步) → RL(交易偏好)

  LLM 的 reward model = 人类打分
  Kronos 的 reward = 风险调整收益（夏普/Sortino/Calmar）

  LLM 的 KL 约束 = 防止模型偏离 SFT 太远
  Kronos 的 KL 约束 = 防止模型只学"全部弃权"的 trivial 策略
```

**防止全弃权的退化解**：在 reward 中加入最小交易率约束：

```python
if trade_rate < min_trade_rate:  # e.g. 20%
    R -= penalty_scale * (min_trade_rate - trade_rate)
```

---

## 4. 置信度校准框架

### 4.1 Reliability Diagram（可靠性图）

无论采用哪种方案，最终都需要在 held-out 数据上验证校准质量：

```
将模型预测按置信度分桶（bin），统计每个桶的实际准确率:

  置信度区间    预测数    实际准确率    是否有价值
  0.50 - 0.60   1200      51.2%       ✗ 无信息量（≈随机）
  0.60 - 0.70    800      54.3%       △ 边缘
  0.70 - 0.80    300      61.7%       ✓ 有价值
  0.80 - 1.00     50      68.0%       ✓✓ 显著

如果高置信度桶的实际准确率显著 > 50%，说明模型的"自信"有意义。
```

### 4.2 校准的置信度来源（按可靠性排序）

| 来源 | 可靠性 | 说明 |
|------|--------|------|
| Ensemble 分歧 | ★★★ 最可靠 | N 个独立模型的预测方差，贝叶斯后验不确定性 |
| Heteroscedastic σ | ★★☆ | 模型自身输出的不确定性，已在训练时学习 |
| Softmax 概率 | ★☆☆ | 原始概率通常 overconfident，需校准后才能用 |
| 方向一致性（多步） | ★★☆ | AR 推理中多步方向是否一致 |

**推荐组合**：Ensemble 分歧 作为主信号 + Heteroscedastic σ 作为辅助特征。

### 4.3 与传统计量模型的对比框架

```
对比实验设计:

  模型 A: ARIMA / GARCH / XGBoost → 每个时间步都预测
  模型 B: Kronos (RL) → 仅输出高置信预测，其余弃权

  评估维度:
    1. 全信号准确率: A 全部预测 vs B 全部预测
    2. 高置信准确率: A 同期 vs B 的高置信子集（最关键）
    3. 信号覆盖率:   B 保留了多少 % 的预测
    4. 信息比率:     accuracy × coverage（准确率与覆盖率的权衡）
    5. 交易收益:     按信号执行的实际风险调整收益

  判定标准:
    如果 B 在维度 2 上显著优于 A（例如 B: 62% vs A: 53%），
    同时维度 3 的覆盖率可接受（丢弃 40-60%），
    则 B 有实用价值。
```

---

## 5. 集成模型方案（与上述方案正交）

### 5.1 多模型 Ensemble 训练

```
训练: 用 3~5 个不同随机种子，分别训练独立模型
推理: 对同一输入，收集所有模型的预测

分歧度 = 预测概率的方差
方向一致性 = 多数投票是否一致
```

### 5.2 两种不确定性类型

| 类型 | 含义 | 捕获方式 | 市场对应 |
|------|------|---------|---------|
| Aleatoric（数据不确定性） | 信号本身就有噪声 | Heteroscedastic σ | 震荡市、无趋势 |
| Epistemic（模型不确定性） | 模型没见过这种模式 | Ensemble 分歧 | 新事件、regime 切换 |

两种不确定性互补，最好同时捕获。

### 5.3 Ensemble 用于 SFT 标签生成

即使最终不部署 ensemble，也可以用它来生成 SFT 阶段的训练标签（§2.1 方法 A）——这是一个不需要在线 ensemble 的低成本用法。

---

## 6. 熵正则化方案（训练时辅助）

### 6.1 自适应熵正则化

**目标**：在训练 loss 中加入条件熵惩罚，让模型在预测错误时学会降低自信度。

```
标准 Cross-Entropy:
  L_CE = -Σ y_c · log(p_c)

自适应熵正则化:
  L = L_CE + β · H(p) · (1 - 2 · correct)

  correct=1 (预测正确): adjustment = -β·H(p) → 鼓励低熵（更确定）
  correct=0 (预测错误): adjustment = +β·H(p) → 鼓励高熵（承认不确定）
```

### 6.2 适用场景

- **适合**：在 SFT 阶段配合三类标签一起使用
- **不适合**：在 PreTrain 阶段直接使用（§0 已说明原因）

### 6.3 与现有 Entropy Alpha 的区别

现有 `--entropy_alpha` 参数是对所有 token 统一施加的无条件熵正则化。自适应版本根据预测正确性动态调整方向——这才是"让模型学到不确定性"的关键。

---

## 7. 实施优先级

| 优先级 | 改进项 | 预估工作量 | 预期收益 | 依赖 |
|:------:|--------|-----------|---------|------|
| P0 | Heteroscedastic 预测头 | 小（改输出层 + loss） | 高（为后续所有步骤提供 σ 信号） | 无 |
| P0 | Ensemble 训练（3 种子） | 中（3x 训练时间） | 高（分歧信号 + SFT 标签来源） | 无 |
| P1 | Reliability Diagram 评估 | 小（评估脚本） | 高（验证校准质量，决定后续方向） | P0 |
| P1 | SFT 三类标签微调 | 中 | 中（RL 的桥接步骤） | P0 ensemble |
| P2 | RL (PPO/DPO) 弃权策略 | 大 | 高（最终目标） | P1 |
| P2 | 与 ARIMA/GARCH 对比实验 | 中 | 高（验证实用价值） | P2 |
| P3 | 自适应熵正则化 | 小 | 中（SFT 辅助） | P1 |
| P3 | MC Dropout 不确定性 | 小 | 中（替代 ensemble 的轻量方案） | 无 |

---

## 8. 风险与注意事项

### 8.1 校准失效风险

即使校准在历史数据上看起来好，也可能是 **regime-specific overfitting**——模型在某个市场阶段的"高置信"确实准确，但换到新阶段就失效。

**缓解**：在多个不同时期的 test set 上分别画 reliability diagram，检查校准的时序稳定性。

### 8.2 弃权率过高

如果训练出的模型弃权率 > 80%，说明模型几乎没有有用的信号，整套方案的 ROI 为负。

**缓解**：设定最小交易率约束（§3.3），在 reward 中惩罚过度弃权。

### 8.3 信息泄漏到置信度

校准评估必须严格隔离——不能用训练集上的置信度来评估，否则会高估校准质量。

**缓解**：所有校准分析基于 held-out test set（cutoff 之后的数据）。

### 8.4 RL 的样本效率

金融数据的信噪比极低，RL 可能需要大量样本才能学到有意义的策略。

**缓解**：先用 SFT 给模型一个合理的初始策略，再用 RL 微调（而非从头 RL）。

---

## 9. 快速验证路径

在投入完整实施之前，可以用最小成本验证核心假设：

```
Step 1: 在现有 checkpoint 上计算 token 概率的直方图
        → 看 softmax 概率分布是否有区分度（是否大部分 > 0.9 或 ≈ uniform）

Step 2: 用 token 概率做简单阈值过滤，在 test set 上画 reliability diagram
        → 如果高概率区间准确率 ≈ 50%，说明现有模型的原始概率没有校准价值
        → 如果高概率区间准确率 > 55%，说明有戏

Step 3: 用 3 个不同种子训练 ensemble，统计分歧度与准确率的相关性
        → 分歧度高的样本准确率是否显著低于分歧度低的样本

如果 Step 2 或 Step 3 显示正信号 → 值得投入完整实施
如果均无信号 → 模型容量或结构可能需要更大的改动
```

---

## 10. 已实施的改进（2026-06-17 BERT 校准）

### 10.1 GPT + BERT 双模型协同范式

**根目录标准工具**（已从 TEMP 提升为生产工具）：
- 模型：`model/kronos_bert.py`（双向 attention + MLM 头）
- 训练：`train_bert.py`（支持 `--dim --depth --heads` 等模型规模 override）
- 评估 V2：`eval_bert_calibration_v2.py`（推荐：GPT 提案 + BERT 验证）
- 评估 V1：`eval_bert_calibration.py`（备选：联合打分）
- 预训练权重：`checkpoints/kronos_bert_big_v1.pt`（推荐，16M，4695 stocks）

完整实验报告：`TEMP/EXP_2026_06_17_BERT_CALIBRATION/REPORT.md`

| 改进 | 状态 | 效果 |
|------|------|------|
| BERT (KronosBert) 实现 | ✅ 已完成 | dim=256~512, depth=2~4, MLM 训练 |
| V1 联合打分 (`eval_bert_calibration.py`) | ✅ 已完成 | DA +1.02pp vs baseline (49.87%) |
| V2 提案-审议 (`eval_bert_calibration_v2.py`) | ✅ 已完成 | DA -0.16pp 但 AR +0.16x, Coll -26pp, Unique +48% |
| 大模型 16M + 全量 4695 stocks | ✅ 已完成 | 容量解锁数据，V2 DA +0.87pp |

### 10.2 关键发现

1. **小 BERT (2.5M) 在 1000 stocks 饱和**——数据增加无收益
2. **大 BERT (16M) 真正利用全量数据**——V2 DA 提升 0.87pp
3. **V2 设计哲学**——GPT 提案，bert_only 联合打分（不是 product）
4. **token 坍塌大幅修复**——V2 14.5% vs baseline 40.7%
5. **幅度坍塌修复**——V2 AmpRatio 1.02x vs baseline 0.86x

### 10.3 待探索方向

- [ ] **多步 AR 验证** — V2 在 1-step 上 work，10-step AR 是否保持？
- [ ] **真正的 future context** — eval 时用真实 future（V3）作为上界
- [ ] **更大 BERT** (32M+) — 是否还有提升空间？
- [ ] **与 thinking 模型组合** — refine_k2 + BERT 校准能否超越两者单独？
- [ ] **多 mask 位置聚合** — boundary 只验证 1 个位置，多位置可能更鲁棒
- [ ] **K sweep** — 系统扫描 K=3,5,10,20 找最优 K
