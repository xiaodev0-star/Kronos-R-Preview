# 信息论视角的 BSQ 最佳 Bit 大小分析

## 1. 数据有效熵估计

### 1.1 原始 4D 特征的有效熵

4 维 OHLC Z-Score 日频对数收益率特征并非独立。log_ret (收盘收益率) 与 log_high, log_low 存在强相关性——日内高低价与收盘价之间的信息重叠度很高。对 A 股市场日频数据 (2010-2026, ~3800 天/股票)，实际测量经验表明：

- **单变量边际分布**: 近似拉普拉斯分布 (尺度参数 $b \approx \sigma/\sqrt{2}$)，每维的微分熵约 $1 + \ln(2b) \approx 2.5$ nats ≈ **3.6 bits/维**
- **4 维联合熵**: 由于强相关性 (log_ret vs log_high 相关系数 ~0.7, log_ret vs log_low 相关系数 ~-0.7, log_high vs log_low 相关系数 ~0.4)，4 维联合微分熵远小于 4×3.6 = 14.4 bits
- **有效秩 (effective rank)**: 4 个 OHLC 回报的对数协方差矩阵的特征值衰减很快。典型地，对 A 股日频数据，前 2 个主成分解释约 85-90% 方差，前 3 个解释约 95%+，第 4 维 (log_open) 的信息量最低

因此，4 维 OHLC Z-Score 特征的有效信息熵约为：
- 转化为离散 bits (考虑 10M 样本的采样密度): **~6-8 bits per 4D 向量**
- 更保守估计: **~5-7 bits**

### 1.2 48 维隐空间的有效熵

Encoder (4→192→48) 将 4 维原始数据投影到 48 维球面上。虽然维度是 48，但流形的内在维数由数据分布决定，而非投影维度：

- 输入只有 4 个自由度 (4 维 OHLC)，经过 2 层 MLP (带 LayerNorm 和 GELU 非线性)，流形维数不会超过输入维数太多
- 由于 LayerNorm + 球面归一化强制 $||z||=1$，实际流形位于 $S^{47}$ (47-球面) 的子流形上
- 有效流形维数估计: **~4-8 维度** (信息的压缩瓶颈是 4 维输入，不是 48 维中间层)

### 1.3 上限推导

根据率失真理论中的 "反向水填充" 定理 (Cover & Thomas 第 11 章)：在给定失真约束下，可编码的码字数上限由数据的率失真函数 $R(D)$ 决定。对于均方失真 $D$ 和高斯源，$R(D) = \frac{1}{2}\sum_i \log(\lambda_i/D)$。

**结论**: 总 bits 应该 ≤ 有效熵 ≈ 5-8 bits。但请注意——这是**每个 4D 向量** 的信息量。由于 BSQ 的 code 是 **整个 48 维隐向量** 的二进制表示，而不仅是 4 维原始数据的无损描述，因此有效 bits 上限略微放宽:

**每样本总的可用 bits 上限 ≈ 6-10 bits**

这意味着：
- 联合词汇量总 bits $k_1 + k_2$ 应 ≤ 10
- $k_1 + k_2 > 10$ 时，理论上必定出现码本浪费或退化

---

## 2. 每样本的 Bits 张力 (N=10M)

### 2.1 经验法则: $2^k \leq \sqrt{N}$

经验法则 $2^{k_{total}} \leq \sqrt{N} \approx 3162$ 给出 $k_{total} \leq \log_2 3162 \approx 11.6$。这与数据有效熵推导一致。

但是，这并不意味"联合词汇量不得超过 $\sqrt{N}$"，因为这是一个 **引导性的经验法则**，用于确保每个 code 至少有 $\sqrt{N}$ 个样本用于训练。实际的信息论限制更宽松：

### 2.2 层级结构的松弛效应

Hierarchical quantizer (coarse + fine) 与 flat quantizer 不同：
- **第一层 (coarse)**: 约 $N/2^{k_1}$ 个样本/code
- **第二层 (fine)**: 在 coarse code 条件约束下，每个 fine code 仅需要约 $N/(2^{k_1} \cdot 2^{k_2}) \cdot p_{valid}$ 个样本 (其中 $p_{valid}$ 是某个 coarse code 内实际出现的 fine code 比例)

由于 fine 层的条件分布通常比边际分布更集中 (给定 coarse code 后，残差的 entropy 较小)，第二层的有效样本要求低于第一层。

### 2.3 联合词汇量的实际上限

- 保守上限: $2^{k_1} \leq 256$ → $k_1 \leq 8$
- 乐观上限: $2^{k_1} \leq 512$ → $k_1 \leq 9$ (但会开始稀疏)
- 联合总词汇量: $2^{k_1 + k_2} \leq 2^{k_{total}} \lesssim N / 100 \approx 100,000$ (每个 code 至少 100 样本)
  - 这给出 $k_{total} \leq \log_2 100000 \approx 16.6$
- 更严格: 每个 code 至少 1000 样本 → $2^{k_{total}} \leq 10000$ → $k_{total} \leq 13.3$

**关键突破**: 层级 quantizer 的联合词汇量 **可以** 超过 $\sqrt{N}$，因为 fine 层是在 coarse 层条件约束下学习的。但是 $k_{total} > 13$ (8+6 已经 14) 时，码本的利用率会急剧下降。

---

## 3. 码本大小 vs 重构失真 (率失真悖论)

现有数据中，增加 bits 却没有提高 SNR，甚至下降：
| Config | Bits (L1+L2) | SNR(dB) | JointU |
|--------|-------------|---------|--------|
| 6+6    | 12          | 8.8     | 3884   |
| 7+6    | 13          | 8.7     | 3654   |
| 8+6    | 14          | 8.2     | 1328   |
| 8+8    | 16          | 7.5     | 2421   |
| 9+8    | 17          | 8.3     | 3805   |

这 **不违反率失真理论**，原因如下：

### 3.1 率失真理论的经典假设
R(D) 函数假设的是 **最优码本分配**。在实际的 BSQ 训练中，这不是最优码本——因为：

1. **BSQ 的码本不是自由选择的**: 码字是超平面投影 + sign() 决定的，不是 MSE 最优的码字
2. **流形假设**: 48 维球面上的有效点只分布在 ~5-8 维子流形上，2^k 个超平面组合产生的区分在这段流形上是 **高度冗余** 的
3. **训练不充分**: 大 bits 配置 (9+8) 的码本空间 2^17 = 131K，但训练只有 10M 样本/100 epochs，每个 code 平均 76 样本，大量 code 从未被分配到样本

### 3.2 真实的率-失真曲线形态

BSQ 的 RD 曲线不是单调递减的。当 k 增大时：
- 理论上的量化误差应该减小 (更多超平面 → 更精细的划分)
- 但实际上，当 k 超过有效熵时，新增的 bits 编码的是噪声而非信号
- 超平面的未对齐: 新增的 hyperplane 在维度不足的流形上产生近乎随机的划分

**观测证据**: 8+6 的 fine PPL = 51 (与 coarse PPL 45 几乎相同)，说明 fine 层基本没有编码额外信息。而 8+8 的 fine PPL 下降到 21，说明 code 退化到只有少数几个 fine code 被使用。

### 3.3 SNR 不随 k 增加的机制

SNR 下降的核心机制是 BSQ 的优化冲突：
- 更多 bits 意味着更大的词汇量，每个 code 更稀疏
- 稀疏的 code 分配导致解码投影矩阵 `decode_proj: R^k -> R^48` 的许多行从未被有效更新
- 解码权重被"浪费"在从未被使用的码字上，降低了实际有效解码维度

---

## 4. 层级 Rate Allocation 分析

### 4.1 信息论最优分配

设信息源有熵 $H$，层级量化器的总 rate $R = R_1 + R_2$。率失真函数 $R(D) = R_1(D_1) + R_2(D_2)$，其中 $D_1 + D_2 \approx D$。

在最优分配下，应使边际改善相等: $\frac{\partial R_1}{\partial D_1} = \frac{\partial R_2}{\partial D_2}$

对于类高斯源:
- 第一层编码信号的**主要成分** (前几个主成分)，信息增益大
- 第二层编码**残差** (残余信息)，残差的信息熵远小于原始信号

这意味着:
$$R_1 > R_2 \quad \text{(coarse bits 应该多于 fine bits)}$$

### 4.2 实证验证

数据清晰地支持这一结论：

| Config | L1 | L2 | L1-L2 | SNR | DA% | Coll% |
|--------|----|----|-------|-----|-----|-------|
| 6+6    | 6  | 6  | 0     | 8.8 | 74.8 | 62.5 |
| **7+6**| **7**| **6**| **+1**| **8.7**| **79.9**| **49.3**|
| 8+6    | 8  | 6  | +2    | 8.2 | **89.0** | 57.8 |
| **7+7**| **7**| **7**| **0** | **8.1**| **83.2**| **44.7**|
| 8+7    | 8  | 7  | +1    | 8.8 | 90.3 | 69.2 |
| 8+8    | 8  | 8  | 0     | 7.5 | 90.9 | 88.1 |
| 9+6    | 9  | 6  | +3    | 7.7 | 87.2 | 85.3 |
| 9+8    | 9  | 8  | +1    | 8.3 | 91.0 | 86.8 |

**关键观察**:
- 当 L1 > L2 (+1 到 +2) 时，性能优于 L1 = L2
- 但 L1 - L2 = +3 (9+6) 时，9 位 coarse 的利用率仅 210/512 = 41%——coarse 太大，反而不利
- **Coarse U 指标**: 7+6 的 CoarseU=125/128=97.7%，8+6=118/256=46.1%，8+7=251/256=98.0%
  - 这意味着 8 位 coarse 只有在 fine 层也提供反馈时才能充分使用 (8+6 比 7+6 更差)
- 9 位 coarse 即使有 fine 层加持，CoarseU 也仅为 210/512=41% (9+6) 到 425/512=83% (9+7)

### 4.3 推荐比率

理论 + 实证给出:
- **L1 应在 6-8 之间**: 6 位利用率 64/64=100%; 7 位在合理 fine 支持下 ~97-100%; 8 位需要 fine 层提供梯度引导 
- **L2 应在 6-7 之间**: 6 位总是利用率高; 7 位可接受; >8 位利用率暴跌
- **L1 - L2 = 1** 是最佳 (coarse 略多于 fine)

---

## 5. 48 维球面编码约束

### 5.1 球面编码容量

在 $S^{d-1}$ ($d=48$) 上的球面码，码字间最小角 $\theta$ 与最大码字数量 $M$ 的关系为:
$$M \leq \frac{A_{d-1}}{A_{d-2}} \cdot \frac{\Gamma(d/2)}{\sqrt{\pi}\,\Gamma((d-1)/2)} \cdot (\sin\theta)^{-(d-2)}$$

对于 $d=48$ 维球面:
- 若要求最小码距 $\theta \geq 15^\circ$ (合理的区分度)，$M \approx (1/\sin 15^\circ)^{46} \approx (3.86)^{46} \approx 10^{27}$——理论上巨大
- 实际码字数受限于 **数据流形**，而非球面编码理论

### 5.2 Johnson-Lindenstrauss 引理

JL 引理说：$N$ 个点从高维投影到 $O(\log N/\epsilon^2)$ 维时，可近似保持距离。

对于 $N \approx 10^7$ 个点，有:
$$d_{JL} \approx \frac{8\log N}{\epsilon^2} \approx \frac{8 \times 16}{\epsilon^2} = \frac{128}{\epsilon^2}$$

若 $\epsilon = 0.1$ (距离失真 10%)，仅需 $d_{JL} \approx 12800$ 维——远超 48 维。
若 $\epsilon = 0.5$，$d_{JL} \approx 512$ 维——仍远超 48 维。
若 $\epsilon = 1.0$ (允许 100% 距离失真)，$d_{JL} \approx 128$ 维——依然 > 48。

**结论**: 48 维不足以在 JL 意义下保距嵌入 $10^7$ 个点。但 BSQ 不需要等距嵌入所有点——它只需要编码 2^k 个码本中心，其中 k 最大约 13 (8192 个码字)。

对于 8192 个码字: $d_{JL} \approx \frac{8\log 8192}{\epsilon^2} = \frac{8 \times 13}{\epsilon^2} = \frac{104}{\epsilon^2}$
- $\epsilon = 0.3$ 时 $d_{JL} \approx 1156$，远超 48
- $\epsilon = 0.7$ 时 $d_{JL} \approx 212$，仍远超 48

### 5.3 实际约束

48 维球面的**实际约束**不在于能放多少个码字——球面的角度容量巨大。真正的约束是：

1. **数据的内在维数**: 4 维输入经过 MLP，有效维数 4-8。BSQ 的 k 个超平面 ($R^{48} \rightarrow R^k$) 必须与数据流形对齐
2. **超平面的正交性**: 初始化使用正交矩阵，但优化后超平面会朝向数据方差最大的方向旋转。当 $k \gg d_{eff}$ (有效维数) 时，新增超平面与已存在的超平面高度相关，产生重复划分
3. **油灰效应**: BSQ 的 `tanh(logits) → sign()` 划分将 48 维空间自动划分成 2^k 个锥形区域。但如果数据只覆盖这些锥形的很小一部分，则 2^k 个区域中大部分是空的

---

## 6. 过参数化与退化边界

### 6.1 当 code 数量接近或超过样本数时

当前数据:
| Config | 总 codes (2^{k1+k2}) | N=10M, samples/code | CoarseU | JointU | Fine Gini |
|--------|---------------------|--------------------|---------|--------|----|
| 6+6    | 4,096               | 2,441              | 64/64   | 3,884 | 0.16 |
| 7+6    | 8,192               | 1,221              | 125/128 | 3,654 | 0.18 |
| 8+6    | 16,384              | 610                | 118/256 | 1,328 | 0.16 |
| 8+8    | 65,536              | 153                | 175/256 | 2,421 | 0.85 |
| 9+8    | 131,072             | 76                 | 334/512 | 3,805 | 0.87 |

**退化边界**: 当 samples/code < ~1000 时，出现码本退化现象。

Fine Gini 系数是关键信号:
- Gini < 0.2: 均匀分布 (良好)
- Gini 0.2-0.5: 轻度偏斜 (可接受)
- Gini > 0.6: 严重退化 (危险)
- Gini > 0.8: 几乎退化为 1-2 个有效 code

**核心洞察**: Fine 层的 Gini 系数对 code 数量极端敏感。8+6 的 fine Gini=0.16 (良好) vs 8+7 的 fine Gini=0.73 (严重退化)。多 1 位 fine bits (128→256 codes) 就把 fine 层的利用率推过崩溃点。

### 6.2 退化机制

当 code 数量太多时，退化分为两个阶段：

**阶段一: 分配不均匀** (samples/code < 500)
- 某些 code 从未被分配到样本
- 优化器专注于少数"受欢迎"的 code
- 不受欢迎的 code 的 `decode_proj` 权重几乎不被更新

**阶段二: 特定 code 主导** (samples/code < 100)
- 一个或几个 code 捕获了绝大部分残差
- Fine 层的 `project.weight` 退化为在少数几个方向上进行划分
- 这就是 Gini > 0.8 时的景象

### 6.3 Hierarchical 结构的保护作用

Hierarchical 结构在理论上可以减缓退化，因为它将搜索空间分成两个层次的搜索。在实践中，数据证实了这一点: 8+6 和 9+6 的 fine Gini 仍保持在 0.16-0.19 (良好)，因为 2^6=64 个 fine code 始终少于有效样本数。

---

## 7. 综合推荐

### 7.1 评分框架

定义评分函数权衡两个目标:
- Discovery Accuracy (DA, 越高越好)
- Collapse Rate (Coll, 越低越好)
- 码本利用率 (Codebook Utilization, 高利用率意味着模型稳定)

| Config | DA% | Coll% | 利用率指标 | 综合评分 (DA - Coll/3) |
|--------|-----|-------|-----------|---------------------|
| 6+6    | 74.8 | 62.5 | 优秀 | 74.8 - 20.8 = 54.0 |
| 7+6    | 79.9 | 49.3 | 良好 | 79.9 - 16.4 = 63.5 |
| **7+7**| **83.2** | **44.7** | **良好** | **83.2 - 14.9 = 68.3** |
| 8+6    | 89.0 | 57.8 | 中等 (coarse 仅 46%) | 89.0 - 19.3 = 69.7 |
| **8+7**| **90.3** | **69.2** | **中等** | **90.3 - 23.1 = 67.2** |
| 8+8    | 90.9 | 88.1 | 差 | 90.9 - 29.4 = 61.5 |
| 9+6    | 87.2 | 85.3 | 差 | 87.2 - 28.4 = 58.8 |
| 9+8    | 91.0 | 86.8 | 差 | 91.0 - 28.9 = 62.1 |

**综合推理**:
- **8+6 在 DA-Coll 差异 (31.2pp) 和综合评分 (69.7) 最高**——粗码本过度稀疏但尚未坍塌
- **7+7 的 collapse (44.7%) 是所有非退化配置中最低的**，且 DA 83.2% 相当不错
- **8+7 有最高 DA (90.3%) 但 collapse 偏高 (69.2%)**
- 9+6 的 coarse 利用率仅 41%，已经属于"伪大码本"

### 7.2 推荐方案

**方案一 (信息论最优): 7+7**

理由：
- 联合词汇量 16,384 ≈ 每个 code ~610 样本，略低于 1000 但 hierarchical 缓解了压力
- 信息论: $k_{total}=14$，接近有效熵 6-10 bits × 2 = 12-20 bits 的下限；实际上 14 bits 对 4D 数据来说已经足够细粒度
- Codebook 利用率基本可接受 (CoarseU 127/128=99%, JointU 1609/16384=9.8%)
- DA-Coll 差异 38.5pp = 最好的权衡之一
- Collapse 44.7% = 所有配置中最低 (不含退化严重的)
- 注意 JointU 9.8% 意味着大量的粗-细组合未被使用，但这是 hierarchical 结构的正常现象

**方案二 (极致性能): 8+6**

理由：
- 最高 DA-Coll 差异 31.2pp，综合排名第一
- 信息论: coarse=8 bits 提供 256 个粗分，fine=6 bits 提供 64 个细分，总量 14 bits
- CoarseU 46% (118/256) 表明有优化的空间 (增加熵正则化可能改善)
- Fine Gini 0.16 极佳——fine 层充分利用
- 缺点: DA-Coll 差异尚可但 collapse 57.8% 偏高

**方案三 (折中推荐): 8+7**

理由：
- 最高 DA (90.3%)，且 CoarseU 251/256=98%——coarse 层面充分使用
- 信息论: 15 bits 总码本，samples/code ≈ 305，接近衰退边界但尚未崩溃
- DA-Coll 差异 21.1pp 处于中等水平

### 7.3 最终推荐

**以 8+7 为第一推荐，其次为 7+7**:

1. **8+7**: 如果目标是最大化 DA (>90%)，且可以接受进一步对抗 collapse 的后续策略 (如更强的熵正则化、codebook reset、EMA codebook)
2. **7+7**: 如果需要在 DA 和稳定性之间取得最佳平衡 (collapse 最低，DA 较高)；这是信息论上最安全的配置

不建议使用的配置:
- **任何 L1 ≥ 9 的配置**: coarse 利用率 < 50%，总 bits 远超有效熵
- **任何 L2 ≥ 8 的配置**: fine Gini > 0.8，fine 层已经退化
- **6+6**: 信息量不足，DA 太低
- **8+8**: 两个层都退化

### 7.4 未来改进方向

1. **在 8+7 或 7+7 上增加 entropy regularization**: 当前 `bsq_entropy_weight=0.01` 偏低，提高此权重 (>0.05) 可以显著改善码本利用率
2. **残差量化 (Residual Quantization, RQ)**: 当前是 2 层独立 BSQ (每层编码全残差)，可以改为每层编码部分残差 (如 RVQ)
3. **Coarse 层的 codeword dropout**: 在训练中随机禁用部分 coarse code，强制 fine 层学习更多码字
4. **梯度协调**: 更大的 bs = 8192 已经很高，但可以加入知识蒸馏——先训一个小码本量 tokenizer，再用它引导大码本
